
// ********RoostGPT********
/*
Test generated by RoostGPT for test demo-java using AI Type Azure Open AI and AI Model roostgpt-4-32k

ROOST_METHOD_HASH=hasNext_846bbf1b43
ROOST_METHOD_SIG_HASH=hasNext_bba7213498


Scenario 1: Validate hasNext method when currentElement is not null

Details:
TestName: hasNextReturnsTrueWhenCurrentElementIsNotNull
Description: This test verifies that the `hasNext` method returns `true` when there is a next element available in the iterator. The `currentElement` field is initialized to be non-null, simulating an active iterator state.

Execution:
Arrange:
- Create an instance of the `Bag` class and add elements to it using the `add` method.
- Obtain an iterator using the `iterator` method.

Act:
- Call the `hasNext` method from the `ListIterator` instance.

Assert:
- Verify that the `hasNext` method returns `true` using JUnit assertions.

Validation:
This test ensures that the iterator correctly identifies the presence of a next element when the `currentElement` field is non-null. It confirms the functionality of the `hasNext` method in typical iterator scenarios and validates its ability to recognize the continuation of iteration.

*/

// ********RoostGPT********

package com.thealgorithms.datastructures.bags;

import org.junit.jupiter.api.Test;
import org.junit.jupiter.api.Tag;
import java.util.Iterator;
import static org.junit.jupiter.api.Assertions.*;
import org.junit.jupiter.api.*;
import java.util.NoSuchElementException;

public class ListIteratorHasNextTest {

	@Test
	@Tag("valid")
	public void hasNextReturnsTrueWhenCurrentElementIsNotNull() {
		// Arrange
		Bag<String> bag = new Bag<>();
		bag.add("FirstElement"); // Adding elements to Bag
		Iterator<String> iterator = bag.iterator(); // Obtaining ListIterator instance
		// Act
		boolean result = iterator.hasNext();
		// Assert
		assertTrue(result, "hasNext should return true when currentElement is not null.");
	}

	@Test
	@Tag("invalid")
	public void hasNextReturnsFalseWhenCurrentElementIsNull() {
		// Arrange
		Bag<String> bag = new Bag<>();
		Iterator<String> iterator = bag.iterator(); // Iterating empty Bag
		// Act
		boolean result = iterator.hasNext();
		// Assert
		assertFalse(result, "hasNext should return false when currentElement is null.");
	}

	@Test
	@Tag("boundary")
	public void hasNextReturnsTrueAtBoundaryWhenOnlyOneElementExists() {
		// Arrange
		Bag<String> bag = new Bag<>();
		bag.add("BoundaryElement"); // Adding single element to Bag
		Iterator<String> iterator = bag.iterator(); // Obtaining ListIterator instance
		// Act & Assert
		assertTrue(iterator.hasNext(), "hasNext should return true when there is one element.");
		iterator.next(); // Move to the next (and only) element
		assertFalse(iterator.hasNext(), "hasNext should return false after the only element is consumed.");
	}

}