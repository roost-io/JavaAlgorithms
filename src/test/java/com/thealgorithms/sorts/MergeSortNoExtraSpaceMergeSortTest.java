
// ********RoostGPT********
/*
Test generated by RoostGPT for test demo-java using AI Type Azure Open AI and AI Model roostgpt-4-32k

ROOST_METHOD_HASH=mergeSort_26b36d9f77
ROOST_METHOD_SIG_HASH=mergeSort_072530dd4b

Here are your existing test cases which we found out and are not considered for test generation:

File Path: C:\var\tmp\Roost\RoostGPT\demo-java\1757415255\source\JavaAlgorithms\src\test\java\com\thealgorithms\sorts\MergeSortRecursiveTest.java
Tests:
    "@Test
void testMergeSortRecursiveCase1() {
        MergeSortRecursive mergeSortRecursive = new MergeSortRecursive(Arrays.asList(5, 12, 9, 3, 15, 88));
        List<Integer> expected = Arrays.asList(3, 5, 9, 12, 15, 88);
        List<Integer> sorted = mergeSortRecursive.mergeSort();
        assertEquals(expected, sorted);
    }
"
    "@Test
void testMergeSortRecursiveCase2() {
        MergeSortRecursive mergeSortRecursive = new MergeSortRecursive(Arrays.asList(-3, 5, 3, 4, 3, 7, 40, -20, 30, 0));
        List<Integer> expected = Arrays.asList(-20, -3, 0, 3, 3, 4, 5, 7, 30, 40);
        List<Integer> sorted = mergeSortRecursive.mergeSort();
        assertEquals(expected, sorted);
    }
"
Scenario 1: Validate sorting of a small array with positive integers.

Details:
  TestName: positiveIntegerArraySorting
  Description: This test verifies that the `mergeSort` method correctly sorts a small array of positive integers in ascending order, leveraging the provided helper methods.
Execution:
  Arrange: Create an array of integers, such as `[5, 3, 8, 1]`. Initialize helper parameters for `mergeSort`, including `start = 0`, `end = array.length - 1`, and `maxele = Arrays.stream(a).max().getAsInt() + 1`.
  Act: Call the `mergeSort` method using these parameters.
  Assert: Assert that the array has been sorted correctly to `[1, 3, 5, 8]`.
Validation:
  The assertion verifies that the `mergeSort` method works as intended for a simple array with distinct positive integers. This is a basic functionality test to confirm the correctness of the algorithm.

---

Scenario 2: Validate sorting of an array that includes duplicate elements.

Details:
  TestName: arrayWithDuplicateElementsSorting
  Description: This test ensures that the `mergeSort` method handles duplicate values appropriately and sorts them in ascending order, maintaining the correct count of duplicates.
Execution:
  Arrange: Create an array, such as `[4, 2, 2, 4, 3]`. Initialize helper parameters for `mergeSort`, including `start = 0`, `end = array.length - 1`, and `maxele = Arrays.stream(a).max().getAsInt() + 1`.
  Act: Invoke the `mergeSort` method.
  Assert: Assert that the resulting sorted array is `[2, 2, 3, 4, 4]`, preserving the correct duplicates.
Validation:
  This test checks if duplicate elements are handled without errors and result in a valid sorted order within the provided array.

---

Scenario 3: Validate sorting of an array with all identical elements.

Details:
  TestName: identicalElementsArraySorting
  Description: This test ensures that the `mergeSort` method does not alter the array when all elements are identical and already sorted.
Execution:
  Arrange: Create an array, such as `[7, 7, 7, 7, 7]`. Set the helper parameters `start = 0`, `end = array.length - 1`, and `maxele = Arrays.stream(a).max().getAsInt() + 1`.
  Act: Call `mergeSort` on the array.
  Assert: Assert that the resulting array remains `[7, 7, 7, 7, 7]`.
Validation:
  This scenario verifies that sorting does not introduce errors or change an already sorted array, even when all values are identical.

---

Scenario 4: Validate sorting of an array with negative integers.

Details:
  TestName: negativeIntegerArraySorting
  Description: This test ensures the `mergeSort` algorithm can handle negative integers correctly and sort them in ascending order.
Execution:
  Arrange: Initialize an array such as `[-5, -2, -8, -1]`. Set `start = 0`, `end = array.length - 1`, and calculate `maxele = Arrays.stream(a).max().getAsInt() + 1`.
  Act: Perform sorting by calling `mergeSort` with the initialized parameters.
  Assert: Assert that the sorted result of the array is `[-8, -5, -2, -1]`.
Validation:
  This test ensures that the algorithm correctly processes negative values and outputs a properly sorted array.

---

Scenario 5: Validate sorting of an array with mixed positive and negative integers.

Details:
  TestName: mixedIntegerArraySorting
  Description: This test checks the ability of the `mergeSort` method to sort an array containing both positive and negative integers in ascending order.
Execution:
  Arrange: Use an array such as `[-3, 5, -1, 0, 4]`. Set `start = 0`, `end = array.length - 1`, and `maxele = Arrays.stream(a).max().getAsInt() + 1`.
  Act: Invoke the `mergeSort` method with the defined parameters.
  Assert: Verify the sorted array output as `[-3, -1, 0, 4, 5]`.
Validation:
  This test confirms the method's capability to manage mixed datasets, including positive and negative integers, ensuring correct sorting order.

---

Scenario 6: Validate sorting of an already sorted array.

Details:
  TestName: alreadySortedArrayDoesNotChange
  Description: This test ensures that when an array is already sorted, `mergeSort` does not introduce any changes or unexpected rearrangements.
Execution:
  Arrange: Create an array such as `[1, 2, 3, 4, 5]`. Set `start = 0`, `end = array.length - 1`, and `maxele = Arrays.stream(a).max().getAsInt() + 1`.
  Act: Call the `mergeSort` method with the initialized parameters.
  Assert: Assert that the returned array remains `[1, 2, 3, 4, 5]`.
Validation:
  This test ensures the algorithm's stability and confirms that pre-sorted arrays are not affected by the sorting process.

---

Scenario 7: Validate sorting of an empty array.

Details:
  TestName: emptyArraySortingTest
  Description: This test checks that the `mergeSort` method gracefully handles an edge case of an empty array without throwing exceptions or errors.
Execution:
  Arrange: Create an empty array, `int[] a = new int[0]`. Set `start = 0`, `end = -1`, and `maxele = 1` (default value for safety).
  Act: Call the `mergeSort` method using these parameters.
  Assert: Confirm that the array remains empty.
Validation:
  This test ensures that the method is robust and does not fail when given an empty array as input.

---

Scenario 8: Validate sorting of a single-element array.

Details:
  TestName: singleElementArraySortingTest
  Description: This test ensures that a single-element array remains unchanged after sorting, as it is already sorted by definition.
Execution:
  Arrange: Create an array such as `[42]`. Set `start = 0`, `end = 0`, and `maxele = Arrays.stream(a).max().getAsInt() + 1`.
  Act: Call the `mergeSort` method with these parameters.
  Assert: Assert that the array remains `[42]`.
Validation:
  This test verifies that the `mergeSort` method handles edge cases of single-element arrays without introducing errors.

*/

// ********RoostGPT********

package com.thealgorithms.sorts;

import org.junit.jupiter.api.Test;
import org.junit.jupiter.api.Tag;
import static org.junit.jupiter.api.Assertions.assertArrayEquals;
import org.junit.jupiter.api.*;
import java.util.Arrays;
import java.util.Scanner;

public class MergeSortNoExtraSpaceMergeSortTest {

	@Test
	@Tag("valid")
	public void positiveIntegerArraySorting() {
		int[] inputArray = { 5, 3, 8, 1 };
		int maxele = Arrays.stream(inputArray).max().getAsInt() + 1;
		MergeSortNoExtraSpace.mergeSort(inputArray, 0, inputArray.length - 1, maxele);
		assertArrayEquals(new int[] { 1, 3, 5, 8 }, inputArray);
	}

	@Test
	@Tag("valid")
	public void arrayWithDuplicateElementsSorting() {
		int[] inputArray = { 4, 2, 2, 4, 3 };
		int maxele = Arrays.stream(inputArray).max().getAsInt() + 1;
		MergeSortNoExtraSpace.mergeSort(inputArray, 0, inputArray.length - 1, maxele);
		assertArrayEquals(new int[] { 2, 2, 3, 4, 4 }, inputArray);
	}

	@Test
	@Tag("valid")
	public void identicalElementsArraySorting() {
		int[] inputArray = { 7, 7, 7, 7, 7 };
		int maxele = Arrays.stream(inputArray).max().getAsInt() + 1;
		MergeSortNoExtraSpace.mergeSort(inputArray, 0, inputArray.length - 1, maxele);
		assertArrayEquals(new int[] { 7, 7, 7, 7, 7 }, inputArray);
	}

	@Test
	@Tag("valid")
	public void negativeIntegerArraySorting() {
		int[] inputArray = { -5, -2, -8, -1 };
		int maxele = Arrays.stream(inputArray).max().getAsInt() + 1; // Adjust for
																		// negative
																		// numbers
		MergeSortNoExtraSpace.mergeSort(inputArray, 0, inputArray.length - 1, maxele);
		assertArrayEquals(new int[] { -8, -5, -2, -1 }, inputArray);
	}

	@Test
	@Tag("valid")
	public void mixedIntegerArraySorting() {
		int[] inputArray = { -3, 5, -1, 0, 4 };
		int maxele = Arrays.stream(inputArray).max().getAsInt() + 1; // Adjust for mixed
																		// numbers
		MergeSortNoExtraSpace.mergeSort(inputArray, 0, inputArray.length - 1, maxele);
		assertArrayEquals(new int[] { -3, -1, 0, 4, 5 }, inputArray);
	}

	@Test
	@Tag("valid")
	public void alreadySortedArrayDoesNotChange() {
		int[] inputArray = { 1, 2, 3, 4, 5 };
		int maxele = Arrays.stream(inputArray).max().getAsInt() + 1;
		MergeSortNoExtraSpace.mergeSort(inputArray, 0, inputArray.length - 1, maxele);
		assertArrayEquals(new int[] { 1, 2, 3, 4, 5 }, inputArray);
	}

	@Test
	@Tag("boundary")
	public void emptyArraySortingTest() {
		int[] inputArray = new int[0];
		int maxele = 1; // Default value for edge case handling
		MergeSortNoExtraSpace.mergeSort(inputArray, 0, -1, maxele);
		assertArrayEquals(new int[0], inputArray);
	}

	@Test
	@Tag("boundary")
	public void singleElementArraySortingTest() {
		int[] inputArray = { 42 };
		int maxele = Arrays.stream(inputArray).max().getAsInt() + 1;
		MergeSortNoExtraSpace.mergeSort(inputArray, 0, 0, maxele);
		assertArrayEquals(new int[] { 42 }, inputArray);
	}

}